command:
  - uvicorn
  - app:app
  - --host
  - 0.0.0.0
  - --port
  - "8000"
env:
  # Genie One MCP needs no space id — it routes across the workspace itself.
  - name: LAKEBASE_HOST
    value: "ep-hidden-fire-e9y4qh66.database.eastus.azuredatabricks.net"
  - name: PGDATABASE
    value: "lead_opt"
  - name: WAREHOUSE_ID
    value: "13d08bfefc608fe6"
